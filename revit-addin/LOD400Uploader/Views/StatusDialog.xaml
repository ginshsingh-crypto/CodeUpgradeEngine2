<Window x:Class="LOD400Uploader.Views.StatusDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LOD 400 - Order Status" 
        Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="#111827">
    
    <Window.Resources>
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E5E7EB"/>
            <Setter Property="BorderBrush" Value="#374151"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1F2937"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#F59E0B"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D97706"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#4B5563"/>
                    <Setter Property="Foreground" Value="#9CA3AF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#111827" BorderBrush="#374151" BorderThickness="0,0,0,1" Padding="24,20">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Order Status" FontSize="24" FontWeight="Bold" Foreground="#F9FAFB"/>
                    <TextBlock Text="Track your orders and download deliverables" FontSize="14" Foreground="#9CA3AF" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Content="Refresh" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Style="{StaticResource ActionButton}" Click="RefreshButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24,16">
            <Border Background="#1F2937" BorderBrush="#374151" BorderThickness="1" CornerRadius="8">
                <ListView x:Name="OrdersListView" BorderThickness="0" Background="Transparent" Foreground="#E5E7EB" HorizontalContentAlignment="Stretch">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Order ID" Width="120" DisplayMemberBinding="{Binding DisplayId}"/>
                            <GridViewColumn Header="Date" Width="140" DisplayMemberBinding="{Binding FormattedDate}"/>
                            <GridViewColumn Header="Sheets" Width="80" DisplayMemberBinding="{Binding SheetCount}"/>
                            <GridViewColumn Header="Total (SAR)" Width="100" DisplayMemberBinding="{Binding TotalPriceSar}"/>
                            <GridViewColumn Header="Status" Width="120">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding StatusColor}" CornerRadius="10" Padding="8,2">
                                            <TextBlock Text="{Binding StatusDisplay}" Foreground="{Binding StatusForeground}" 
                                                       FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Action" Width="140">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Download" Style="{StaticResource PrimaryButton}"
                                                Visibility="{Binding CanDownload, Converter={StaticResource BoolToVis}}"
                                                Click="DownloadButton_Click" Tag="{Binding Id}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
            
            <Grid x:Name="LoadingOverlay" Background="#CC111827" Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Loading orders..." FontSize="16" Foreground="#F9FAFB"/>
                </StackPanel>
            </Grid>
            
            <StackPanel x:Name="EmptyState" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                <TextBlock Text="No orders found" FontSize="18" FontWeight="SemiBold" Foreground="#E5E7EB" HorizontalAlignment="Center"/>
                <TextBlock Text="Upload sheets to create your first order." FontSize="14" Foreground="#9CA3AF" Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="#111827" BorderBrush="#374151" BorderThickness="0,1,0,0" Padding="24,16">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" Style="{StaticResource ActionButton}" Width="80" Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
